REVERSE function

+-----------------------------------------------------------------------------+
| REVERSE(string)                                                             |
+-----------------------------------------------------------------------------+

Returns the string with the characters in reversed order.

Examples:

	REVERSE('ABc .')       == '. cBA'
	REVERSE(' XYZ')        == ' ZYX'
	REVERSE('Tranquility') == 'ytiliuqnarT'
