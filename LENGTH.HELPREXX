LENGTH function

+-----------------------------------------------------------------------------+
| LENGTH(string)                                                              |
+-----------------------------------------------------------------------------+

Returns the length of the string.

Examples:

	LENGTH('abcdefgh') == 8
	LENGTH('')         == 0
